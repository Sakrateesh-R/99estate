# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — never commit real secrets
.env
.env.local
.env*.local

# typescript
*.tsbuildinfo

# supabase local dev
/supabase/.branches
/supabase/.temp
/supabase/.env

# vercel
.vercel

# local screenshots / browser automation scratch
/.playwright-mcp
/*.png

# machine-local Claude Code state (paths + session ids, not shareable).
# A shared .claude/settings.json would still be committed.
.claude/settings.local.json
